%**Vector Operation:**
%Create a MATLAB script that defines a vector `v` containing the numbers 
% 1 through 5, then calculates and displays the sum of the elements in `v`.

v = 1:5;
disp(v);
disp(sum(v));